# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env
.python-version

# Data & model artifacts (regenerated by ingest / train)
data/raw/
data/interim/
data/processed/
data/snapshots/
models/registry/*
# ...but DO commit the trained model so a fresh clone / Docker image ships something
# that works (swap in the Colab 3-target model when ready).
!models/registry/.gitkeep
!models/registry/lgbm_*.txt
!models/registry/*_meta.json
!models/registry/registry_index.json
!models/registry/backtest_metrics.json

# ui-ux-pro-max / design skills installed by `uipro init` (not project source)
.claude/skills/

# training run output
vayucast_model.zip


# Notebooks
.ipynb_checkpoints/

# WRF-Chem outputs (huge)
wrfchem/run/
wrfchem/*.nc
wrfchem/wrfout_*

# Node / frontend
web/node_modules/
web/dist/
web/.vite/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# gsd-core staging
.claude/.gsd-staging/
